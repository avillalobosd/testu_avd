<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg2}}</h1>
        <h2>Toma el Exámen</h2>
        <button id="logOut" class="btn btn-primary float-right">Logout</button>

          {{#each curso}} 
      

          <li data-id="{{this.id}}" class="list-group-item">
            
            <p>{{this.pregunta}}</p>
        
<form>
            {{#each ../respuestas}} 
            
            {{#if_eq this.idPreguntas ../this.id}}
            
            <input name="resp" type="radio" id="{{this.id}}">{{this.respuesta}}</br>
            {{/if_eq}} 
            
            {{/each}}
</form>

         
          {{/each}}

        </ul>
     <button data-examen="{{examen}}"" id="submit" class="btn btn-primary allign-left">Enviar</button>
      </div>
   
    </div>
  
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/examen.js"></script>