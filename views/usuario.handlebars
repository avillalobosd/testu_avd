<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg2}}</h1>
        <h2>This is the Wall!</h2>
        <button id="logOut" class="btn btn-primary float-right">Logout</button>


       
        <h3>Messages:</h3>
        <ul id="example-list" class="list-group">
           {{#each examanes}}
          
            <li data-id="{{this.id}}"class="list-group-item">
            <button data-id="{{this.id}}" id="{{this.id}}"class="examen btn btn-secondary" type="button">Tomar Examen</button>
            <button data-id="{{this.id}}" id="{{this.id}}"class="curso btn btn-info" type="button">Archivos del Curso</button>
  
            {{!-- <button class="btn btn-danger float-right delete">ｘ</button> --}}
           </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/usuario.js"></script>
